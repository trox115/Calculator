import React from 'react'
import PropTypes from 'prop-types'
const Button = (props) =>{
    const btname = props
        return(
            <button className = 'calc'>
        );
}
Button.propTypes = {
    btname:PropTypes.string
}
